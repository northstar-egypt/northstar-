# Dependencies for data/pipelines: the ingestion adapters and the synthetic data
# generator. Deliberately separate from apps/api/requirements.txt so the API
# image never carries pandas, Playwright or a browser runtime it does not use.
#
# Pinned exactly, rather than to a minor version as the API does: the synthetic
# generator is meant to be reproducible from a seed, and a Faker or pandas
# upgrade can change what a given seed produces.

pandas==2.2.3
faker==24.0.0
playwright==1.42.0

# Needed by data/pipelines/synthetic, which builds rows as SQLAlchemy model
# instances from apps/api rather than writing dictionaries. psycopg is required
# even for a JSON-only run, because importing app.db constructs an Engine.
sqlalchemy==2.0.52
psycopg[binary]==3.3.4
